image:
  repository: nginx
  tag: latest

ports:
  - name: http
    containerPort: 8080

service:
  main:
    port: 8080

podDisruptionBudget:
  enabled: true
  minAvailable: 1
  annotations:
    test.example.com/enabled: "true"
  labels:
    app.kubernetes.io/component: budget
